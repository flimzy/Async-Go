package Async::Go::Channel;
use strict;
use warnings;

use Carp qw( croak );

sub type_of {
    my ( $self, $value ) = @_;
    # TODO
}

sub is_type {
    my ( $self, $value, $type ) = @_;
    # TODO
}

1;
